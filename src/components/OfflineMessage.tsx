import React from 'react';

const OfflineMessage = () => (
  <div className="offline-container flex items-center justify-center py-20 bg-red-50 text-red-700 rounded-lg shadow">
    <p className="text-xl font-medium"> hors ligne </p>
  </div>
);

export default OfflineMessage;